module MathOperation
    def sum(first_value, second_value)
        total = first_value + second_value
        puts "#{first_value} + #{second_value} = #{total}"
    end

    def product(first_value, second_value)
        total = first_value * second_value
        puts "#{first_value} * #{second_value} = #{total}"
    end
end